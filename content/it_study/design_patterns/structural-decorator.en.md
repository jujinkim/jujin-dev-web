---
publish: false
lang: en
title: "Decorator Pattern"
---


If general inheritance is inheriting a blueprint,
the decorator pattern is inheriting an instantiated object (implementation).
Kotlin's `by delegation` implements the decorator at the language level (removes boilerplate).
