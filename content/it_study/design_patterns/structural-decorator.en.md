---
publish: false
lang: en
title: "Decorator Pattern"
---
```markdown

If general inheritance inherits blueprints,
the decorator inherits the created object (implementation).
Kotlin's `by` delegation implements the decorator at the language level (eliminating boilerplate).
```
